class Solution {
public:
    void reverseString(vector<char>& str) {
        // for (int i = str.size()-1; i >= 0; i--)
        // cout << str[i];
        reverse(str.begin(), str.end());
       
        
        
    }
};